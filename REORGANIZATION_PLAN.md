# 📋 План реорганізації проекту FERM Bot

## Поточна ситуація:
- ✅ `bot/` - новий проект (покрокова розробка)
- ⚠️ `core/` - старий проект (референс)
- ⚠️ Кореневі файли (Dockerfile, Makefile, docker-compose.yml, README.md) - для старого проекту

## Мета:
Зробити `bot/` основним проектом, залишити `core/` як референс

---

## 📌 Крок 1: Переміщення старих файлів

### Що зробити:
```bash
# Створити директорію для старого проекту
mkdir -p core_reference/

# Перемістити файли старого проекту
mv core/ core_reference/core/
mv Dockerfile core_reference/
mv docker-compose.yml core_reference/
mv Makefile core_reference/
mv README.md core_reference/README_OLD.md
```

### Результат:
```
ferm-bot/
├── bot/                  # Новий проект
├── core_reference/       # Старий проект (архів)
│   ├── core/
│   ├── Dockerfile
│   ├── docker-compose.yml
│   ├── Makefile
│   └── README_OLD.md
├── pyproject.toml        # Poetry для bot/
└── .gitignore
```

---

## 📌 Крок 2: Створення нового README.md

Створити новий README.md для bot проекту в корені

---

## 📌 Крок 3: Налаштування Poetry

```bash
# Встановити залежності (створить .venv та poetry.lock)
poetry install

# Перевірити
poetry env info
```

---

## 📌 Крок 4: Оновлення .env.example

Створити простий .env.example для bot проекту (тільки BOT_TOKEN поки що)

---

## 📌 Крок 5: Git коміт

```bash
git add .
git commit -m "Reorganize: move old core to core_reference, prepare for bot development"
git push
```

---

## 🎯 Фінальна структура:

```
ferm-bot/
├── 📁 bot/                    # НОВИЙ проект (основний)
│   ├── __init__.py
│   ├── main.py
│   └── README.md
│
├── 📁 core_reference/         # Старий проект (тільки для довідки)
│   ├── core/
│   ├── Dockerfile
│   ├── docker-compose.yml
│   ├── Makefile
│   └── README_OLD.md
│
├── 📁 .venv/                  # Віртуальне середовище (буде створене)
├── 📁 .git/                   # Git репозиторій
│
├── 📄 pyproject.toml          # Poetry конфігурація
├── 📄 poetry.lock             # Poetry lock (буде створений)
├── 📄 .gitignore              # Git ignore
├── 📄 .env.example            # Приклад конфігурації для bot
├── 📄 .env                    # Ваші налаштування (не в git)
└── 📄 README.md               # Новий README для bot проекту
```

---

## ✅ Переваги такої структури:

1. **bot/** - основний проект, з ним працюємо
2. **core_reference/** - старий код як довідка (не заважає)
3. **Корінь** - чистий, тільки для bot проекту
4. **Poetry** - керує залежностями для bot
5. **.venv** - ізольоване середовище для bot

---

## 🚀 Команди для роботи:

```bash
# Встановити залежності
poetry install

# Активувати середовище
poetry shell

# Запустити бота
poetry run python -m bot.main

# Або в активованому середовищі:
python -m bot.main
```
