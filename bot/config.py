"""
КРОК 2: Конфігурація бота через .env файл

Що таке конфігурація?
- Це налаштування які можуть змінюватись (токени, URL, параметри)
- Не повинні бути в коді (небезпечно!)
- Зберігаються в .env файлі

Що таке .env файл?
- Текстовий файл з парами ключ=значення
- НЕ в Git (щоб не злити токени)
- Кожен розробник має свій .env

Приклад .env:
    BOT_TOKEN=123456789:ABCdefGHIjklMNOpqrsTUVwxyz
    DEBUG=True
    LOG_LEVEL=INFO

Що таке Pydantic Settings?
- Бібліотека для читання і валідації конфігурації
- Автоматично читає з .env
- Перевіряє типи даних
- Дає зрозумілі помилки
"""

from pathlib import Path
from pydantic_settings import BaseSettings, SettingsConfigDict

# ========================================
# ВИЗНАЧАЄМО КОРІНЬ ПРОЕКТУ
# ========================================
# __file__ - це шлях до поточного файла (bot/config.py)
# .parent - це директорія bot/
# .parent.parent - це корінь проекту (де .env)
PROJECT_ROOT = Path(__file__).parent.parent


# ========================================
# КЛАС НАЛАШТУВАНЬ
# ========================================
class Settings(BaseSettings):
    """
    Клас з налаштуваннями бота

    Наслідується від BaseSettings з Pydantic
    Автоматично читає змінні з .env файлу

    Як це працює:
    1. Ви створюєте об'єкт: settings = Settings()
    2. Pydantic шукає .env файл
    3. Читає BOT_TOKEN з .env
    4. Перевіряє що це string (str)
    5. Якщо немає - помилка!
    """

    # ========================================
    # ОБОВ'ЯЗКОВІ НАЛАШТУВАННЯ
    # ========================================

    BOT_TOKEN: str
    """
    Токен Telegram бота від @BotFather

    Тип: str (string, текст)
    Обов'язковий: так (немає значення за замовчуванням)

    Якщо не вказати BOT_TOKEN в .env - буде помилка:
    "Field required [type=missing, input_value={}, input_type=dict]"

    Приклад в .env:
    """

    # ========================================
    # ОПЦІОНАЛЬНІ НАЛАШТУВАННЯ (мають значення за замовчуванням)
    # ========================================

    DEBUG: bool = False
    """
    Режим відладки (debug mode)

    Тип: bool (True/False)
    За замовчуванням: False

    Якщо True:
    - Більше логів
    - Детальні помилки
    - Не для продакшену!

    Приклад в .env:
        DEBUG=True
    """

    LOG_LEVEL: str = "INFO"
    """
    Рівень логування

    Тип: str
    За замовчуванням: "INFO"

    Можливі значення:
    - "DEBUG" - всі логи (багато)
    - "INFO" - інформаційні (норма)
    - "WARNING" - попередження
    - "ERROR" - тільки помилки

    Приклад в .env:
        LOG_LEVEL=DEBUG
    """

    LOG_TO_FILE: bool = True
    """
    Зберігати логи у файл (КРОК 3)

    Тип: bool (True/False)
    За замовчуванням: True

    Якщо True:
    - Логи зберігаються в logs/bot.log
    - Автоматична ротація (новий файл кожні 10 MB)
    - Зберігаються 1 тиждень
    - Стискаються в zip

    Якщо False:
    - Логи тільки в консолі (не зберігаються)

    Приклад в .env:
        LOG_TO_FILE=True
    """

    DATABASE_URL: str = f"sqlite+aiosqlite:///{PROJECT_ROOT}/ferm_bot.db"
    """
    URL для підключення до бази даних (КРОК 4)

    Тип: str
    За замовчуванням: SQLite файл у корені проекту

    Формат:
    - SQLite: sqlite+aiosqlite:///path/to/database.db
    - PostgreSQL: postgresql+asyncpg://user:password@localhost/dbname

    Приклад в .env:
        DATABASE_URL=sqlite+aiosqlite:///ferm_bot.db
        # або
        DATABASE_URL=postgresql+asyncpg://user:pass@localhost/ferm_bot

    Що означають частини:
    - sqlite+aiosqlite:// - тип БД + async драйвер
    - ///path/to/db - шлях до файлу БД (3 слеші для абсолютного шляху)
    """

    OPENAI_API_KEY: str | None = None
    """
    API ключ для OpenAI (для AI-консультацій)

    Тип: str | None (опціонально)
    За замовчуванням: None (AI відключений)

    Якщо вказано:
    - Увімкнені AI-консультації по товарах
    - Використовується GPT для рекомендацій

    Якщо None:
    - AI-консультації недоступні
    - Кнопка "Консультація з ШІ" не показується

    Приклад в .env:
        OPENAI_API_KEY=sk-proj-...
    """

    OPENAI_MODEL: str = "gpt-4o-mini"
    # OPENAI_MODEL: str = "gpt-5"
    """
    Модель OpenAI для використання

    Тип: str
    За замовчуванням: "gpt-4o-mini" (швидка і дешева)

    Можливі варіанти:
    - "gpt-4o-mini" - швидка, дешева (рекомендовано)
    - "gpt-4o" - найпотужніша
    - "gpt-3.5-turbo" - старіша, дешевша

    Приклад в .env:
        OPENAI_MODEL=gpt-4o-mini
    """

    # ========================================
    # КОНФІГУРАЦІЯ PYDANTIC
    # ========================================

    model_config = SettingsConfigDict(
        # Звідки читати змінні
        # ВАЖЛИВО: Використовуємо абсолютний шлях до .env
        # Це працює незалежно від того, звідки запущено скрипт
        env_file=str(PROJECT_ROOT / ".env"),  # /home/user/ferm-bot/.env
        env_file_encoding="utf-8",            # Кодування UTF-8 (для кирилиці)

        # Що робити з зайвими полями в .env
        extra="ignore",               # Ігнорувати (не падати з помилкою)

        # Чутливість до регістру
        case_sensitive=False          # BOT_TOKEN = bot_token = Bot_Token
    )


# ========================================
# СТВОРЕННЯ ОБ'ЄКТА НАЛАШТУВАНЬ
# ========================================
# Це відбудеться ОДРАЗУ при імпорті модуля
# Python прочитає .env файл і створить об'єкт settings

try:
    settings = Settings()
    # Якщо все ОК - settings містить налаштування з .env

except Exception as e:
    # Якщо помилка (наприклад, немає .env або BOT_TOKEN)
    print("❌ ПОМИЛКА КОНФІГУРАЦІЇ!")
    print(f"   {e}")
    print()
    print("💡 Можливі причини:")
    print("   1. Немає файлу .env в корені проекту")
    print("   2. В .env немає BOT_TOKEN")
    print("   3. .env файл порожній або неправильно заповнений")
    print()
    print("🔧 Як виправити:")
    print(f"   1. Перевірте що .env файл існує: {PROJECT_ROOT / '.env'}")
    print(f"   2. Створіть з прикладу: cp {PROJECT_ROOT / '.env.example'} {PROJECT_ROOT / '.env'}")
    print("   3. Додайте токен: BOT_TOKEN=ваш_токен_від_BotFather")
    print()
    print("📍 .env файл має бути тут:")
    print(f"   {PROJECT_ROOT / '.env'}")
    print()
    raise  # Передаємо помилку далі (зупиняє програму)


# ========================================
# ЯК ВИКОРИСТОВУВАТИ
# ========================================
"""
В інших файлах імпортуйте settings:

    from bot.config import settings

    # Отримати токен
    token = settings.BOT_TOKEN

    # Перевірити режим
    if settings.DEBUG:
        print("Режим відладки увімкнений")

    # Отримати рівень логів
    log_level = settings.LOG_LEVEL

Переваги:
✅ Токени не в коді (безпека!)
✅ Автоматична валідація типів
✅ Зрозумілі помилки
✅ Легко змінювати налаштування
✅ Різні налаштування для dev/prod
"""
