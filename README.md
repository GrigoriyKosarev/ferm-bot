# 🌾 FERM Telegram Bot

> Агротехнологічний Telegram-бот для платформи FERM - ваш особистий агропомічник

[![Python 3.10+](https://img.shields.io/badge/python-3.10+-blue.svg)](https://www.python.org/downloads/)
[![Aiogram 3.x](https://img.shields.io/badge/aiogram-3.x-green.svg)](https://docs.aiogram.dev/)
[![License: MIT](https://img.shields.io/badge/License-MIT-yellow.svg)](https://opensource.org/licenses/MIT)

---

## 📋 Зміст

- [Про проект](#-про-проект)
- [Можливості](#-можливості)
- [Швидкий старт](#-швидкий-старт)
- [Встановлення](#-встановлення)
- [Конфігурація](#-конфігурація)
- [Запуск](#-запуск)
- [Структура проекту](#-структура-проекту)
- [API інтеграції](#-api-інтеграції)
- [Розробка](#-розробка)
- [Deployment](#-deployment)
- [FAQ](#-faq)

---

## 🎯 Про проект

**FERM Bot** - це інтелектуальний помічник для аграріїв України, який допомагає:
- Підібрати насіння, добрива та засоби захисту рослин
- Отримати агропогоду з рекомендаціями для робіт
- Розрахувати норми застосування препаратів
- Знайти актуальні гранти та програми підтримки
- Орендувати сільськогосподарську техніку

### Технічний стек

- **Backend**: Python 3.10+, Aiogram 3.x
- **Database**: SQLite (dev), PostgreSQL (prod)
- **Cache**: Redis
- **AI**: OpenAI GPT-4 для консультацій
- **Weather**: AccuWeather API
- **Deployment**: Docker, Docker Compose

---

## ✨ Можливості

### 🛒 Каталог товарів
- Три основні категорії: **Насіння**, **Добрива**, **ЗЗР**
- Підкатегорії для точного підбору
- Детальна інформація про кожен товар
- Фото, ціни, наявність
- Кошик з можливістю редагування
- Перехід на сайт для оплати

### 🌤 АгроПогода
- Прогноз погоди на 5 днів
- Агрорекомендації на основі погодних умов
- Визначення сприятливих днів для робіт
- Попередження про ризики
- Підписка на щоденні розсилки

### 💡 ШІ-Консультант
- Підбір оптимальних товарів
- Розрахунок норм застосування
- Відповіді на агрономічні питання
- Рекомендації супутніх товарів
- Історія консультацій

### 💰 АгроГранти
- Актуальні грантові програми
- Онлайн подача заявок
- Консультація по документах
- Підписка на новини про гранти

### 🚜 АгроУклон (Оренда техніки)
- Каталог доступної техніки
- Швидка заявка на оренду
- Розрахунок вартості
- Можливість доставки

---

## 🚀 Швидкий старт

### Вимоги

- Python 3.10 або вище
- Poetry (менеджер залежностей)
- Telegram Bot Token від @BotFather

### 3 команди для запуску

```bash
# 1. Клонувати та налаштувати
git clone <your-repo>
cd ferm-telegram-bot
make setup

# 2. Додати токен в .env
nano .env  # Вставте BOT_TOKEN

# 3. Запустити
make run
```

Готово! Бот запущено 🎉

---

## 📦 Встановлення

### 1. Встановити Poetry

```bash
# Linux/macOS
curl -sSL https://install.python.poetry.org | python3 -

# Windows PowerShell
(Invoke-WebRequest -Uri https://install.python.org -UseBasicParsing).Content | python -
```

### 2. Клонувати репозиторій

```bash
git clone <repository-url>
cd ferm-telegram-bot
```

### 3. Встановити залежності

```bash
# Автоматично (рекомендовано)
make setup

# Або вручну
poetry install
cp .env.example .env
```

---

## ⚙️ Конфігурація

### Створення бота в Telegram

1. Відкрийте [@BotFather](https://t.me/BotFather)
2. Відправте `/newbot`
3. Введіть ім'я та username бота
4. Збережіть отриманий токен

### Налаштування .env файлу

```bash
# Обов'язкові параметри
BOT_TOKEN=123456789:ABCdefGHIjklMNOpqrsTUVwxyz  # Від BotFather

# FERM API (зверніться до адміністрації)
FERM_API_URL=https://api.ferm.in.ua
FERM_API_KEY=your_api_key

# База даних (SQLite для розробки)
DATABASE_URL=sqlite+aiosqlite:///./ferm_bot.db

# Опціональні параметри
OPENAI_API_KEY=sk-...          # Для ШІ-консультацій
ACCUWEATHER_API_KEY=...        # Для погоди
REDIS_URL=redis://localhost:6379  # Для кешування
```

---

## 🏃 Запуск

### Локально

```bash
# Простий запуск
make run

# В режимі розробки (з DEBUG)
make dev

# З власними командами
poetry run python -m core.bot
```

### Docker

```bash
# Запуск всього стеку (bot + PostgreSQL + Redis)
make docker-run

# Або через docker-compose
docker-compose up -d

# Перегляд логів
docker-compose logs -f bot

# Зупинка
docker-compose down
```

---

## 📂 Структура проекту

```
ferm-telegram-bot/
├── core/                          # Основний код
│   ├── bot.py                     # Точка входу
│   ├── config.py                  # Налаштування
│   ├── handlers/                  # Обробники команд
│   │   ├── start.py              # /start, головне меню
│   │   ├── catalog.py            # Каталог товарів
│   │   ├── cart.py               # Кошик
│   │   ├── weather.py            # АгроПогода
│   │   ├── grants.py             # АгроГранти
│   │   └── consultation.py       # ШІ-консультації
│   ├── keyboards/                 # Клавіатури
│   │   ├── inline.py             # Inline кнопки
│   │   └── reply.py              # Reply кнопки
│   ├── services/                  # Бізнес-логіка
│   │   ├── ferm_api.py           # API FERM
│   │   ├── weather_api.py        # AccuWeather
│   │   └── ai_service.py         # OpenAI
│   └── database/                  # База даних
│       ├── models.py             # Моделі SQLAlchemy
│       ├── database.py           # Підключення
│       └── queries.py            # CRUD операції
├── tests/                         # Тести
├── logs/                          # Логи
├── pyproject.toml                 # Залежності
├── .env.example                   # Приклад конфігурації
├── Dockerfile                     # Docker образ
├── docker-compose.yml             # Docker Compose
├── Makefile                       # Команди автоматизації
└── README.md                      # Ця документація
```

### Ключові модулі

#### Handlers (Обробники)
- **start.py** - Команда /start, головне меню, навігація
- **catalog.py** - Каталог товарів, категорії, пошук, розрахунки
- **cart.py** - Кошик, додавання/видалення товарів
- **weather.py** - Погода, агророзсилки (в розробці)
- **grants.py** - Заявки на гранти (в розробці)
- **consultation.py** - ШІ-консультації (в розробці)

#### Services (Сервіси)
- **ferm_api.py** - Інтеграція з API FERM (товари, акції)
- **weather_api.py** - AccuWeather API (погода)
- **ai_service.py** - OpenAI для консультацій

#### Database (База даних)
- **models.py** - SQLAlchemy моделі (User, CartItem, GrantApplication тощо)
- **queries.py** - CRUD операції

---

## 🔌 API Інтеграції

### FERM API

```python
from core.services.ferm_api import FermAPI

api = FermAPI()

# Отримати товари
products = await api.get_products(category="seeds", page=1)

# Деталі товару
product = await api.get_product(product_id=101)

# Акції
promotions = await api.get_promotions(limit=10)
```

### OpenAI (ШІ-консультант)

```python
from core.services.ai_service import AIService

ai = AIService()

# Консультація
response = await ai.get_consultation(
    user_message="Яке насіння обрати для озимої пшениці?",
    context={"region": "Київська область"}
)
```

### AccuWeather (Погода)

```python
from core.services.weather_api import WeatherAPI

weather = WeatherAPI()

# Прогноз
forecast = await weather.get_forecast(location_key="324505")

# Агророзсилки
recommendations = await weather.get_agro_recommendations(forecast)
```

---

## 🛠 Розробка

### Команди

```bash
# Форматування коду
make format

# Перевірка коду
make lint

# Запуск тестів
make test

# Очищення
make clean
```

### Додавання нового функціоналу

1. Створіть новий handler в `core/handlers/`
2. Додайте роутер в `core/bot.py`
3. Створіть клавіатури в `core/keyboards/`
4. При потребі додайте моделі БД в `core/database/models.py`

### Структура handler'а

```python
from aiogram import Router, F
from aiogram.types import Message

router = Router(name="my_feature")

@router.message(F.text == "Моя кнопка")
async def my_handler(message: Message):
    await message.answer("Відповідь")
```

### Робота з БД

```python
from core.database.database import AsyncSessionLocal
from core.database.queries import get_user

async with AsyncSessionLocal() as session:
    user = await get_user(session, user_id=123)
```

---

## 🚢 Deployment

### VPS (Ubuntu/Debian)

```bash
# 1. Встановити Docker
curl -fsSL https://get.docker.com | sh

# 2. Клонувати проект
git clone <repo>
cd ferm-telegram-bot

# 3. Налаштувати .env
cp .env.example .env
nano .env  # Додати токени

# 4. Запустити
docker-compose up -d

# Перегляд логів
docker-compose logs -f bot
```

### Systemd Service

Створити `/etc/systemd/system/ferm-bot.service`:

```ini
[Unit]
Description=FERM Telegram Bot
After=network.target

[Service]
Type=simple
User=your-user
WorkingDirectory=/path/to/ferm-telegram-bot
ExecStart=/usr/local/bin/poetry run python -m core.bot
Restart=always

[Install]
WantedBy=multi-user.target
```

```bash
sudo systemctl daemon-reload
sudo systemctl enable ferm-bot
sudo systemctl start ferm-bot
```

---

## ❓ FAQ

### Бот не запускається

**Перевірте:**
1. Правильність BOT_TOKEN в .env
2. Чи встановлені залежності: `poetry install`
3. Логи помилок в консолі

### Помилка бази даних

```bash
# Переініціалізувати БД
make db-reset
```

### API FERM недоступний

Бот використовує mock data (заглушки) якщо API недоступний. Функціонал працюватиме з тестовими даними.

### Як додати нову категорію товарів?

Відредагуйте `core/config.py`, додайте в `CATEGORIES`.

---

## 📞 Підтримка

- **Email**: admin@ferm.in.ua
- **Telegram**: @ferm_support
- **Сайт**: [ferm.in.ua](https://ferm.in.ua)
- **GitHub Issues**: [Створити issue](https://github.com/your-repo/issues)

---

## 📄 Ліцензія

MIT License - дивіться [LICENSE](LICENSE)

---

## 🙏 Подяки

Створено з ❤️ для аграріїв України 🇺🇦

**Made with:**
- [Aiogram](https://docs.aiogram.dev/) - Telegram Bot framework
- [SQLAlchemy](https://www.sqlalchemy.org/) - ORM
- [OpenAI](https://openai.com/) - AI консультації
- [AccuWeather](https://www.accuweather.com/) - Погодні дані

---

⭐ Якщо проект корисний - поставте зірку на GitHub!